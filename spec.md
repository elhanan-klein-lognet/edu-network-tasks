# מסמך אפיון - מערכת ניהול משימות לרשת מוסדות חינוכיים

**גרסה:** 1.4 (הוספת הפרדת סביבות פיתוח/טסט מול production)
**מטרת השלב הנוכחי:** בניית POC (הוכחת היתכנות) להצגה בפני מנהל הרשת

---

## מה השתנה מגרסה 1.3

נוסף לסעיף 9 (ארכיטקטורה) הסבר על הפרדת סביבות עבודה: ניצול Preview/Production Deployments המובנים ב-Vercel, לצד שני פרויקטי Supabase נפרדים (production ו-dev/test משותף), כדי שפיתוח ובדיקות לא יגעו בנתוני האמת.

---

## 1. רקע ומטרה

מערכת לניהול משימות עבור צוות ההנהלה של רשת מוסדות חינוכיים, בסגנון Monday.com/Jira אך בהיקף מצומצם ומותאם לצרכים בפועל. המערכת נבנית תחילה כ-POC על תשתית חינמית, במטרה להדגים את הערך למנהל הרשת לפני מעבר לגרסה מלאה בתשלום.

**היקף משתמשים:** 10-30 משתמשים (הנהלת רשת + מנהלי מוסדות + עובדים)

**דרישות ממשק:** הממשק יהיה בעברית מלאה, כולל תמיכת RTL. לשלב ה-POC, המערכת תהיה web בלבד ותומכת בתצוגה מותאמת (responsive) למחשב וטאבלט/מובייל בדפדפן - ללא אפליקציה נייטיבית.

---

## 2. משתמשים והרשאות

ארבעה תפקידים במערכת:

| תפקיד | הרשאות |
|---|---|
| **מנהל-על (Super Admin)** | גישה לכל הרשת, ניהול משתמשים, יצירת boards, כל ההרשאות |
| **הנהלת רשת** | רואה הכל, יוצר משימות ופרויקטים כלל-רשתיים, גישה לדשבורד המלא |
| **מנהל מוסד** | רואה את ה-board של המוסד שלו + משימות כלל-רשתיות שהוקצו אליו, מקצה משימות בתוך המוסד |
| **עובד/חבר צוות** | רואה רק את המשימות שהוקצו לו אישית |

ההפרדה בין מוסדות תיאכף ברמת בסיס הנתונים (Row Level Security ב-Supabase), כך שלא ניתן "לעקוף" אותה בטעות מקוד האפליקציה.

בפרויקט כלל-רשתי שמכיל משימות ממספר מוסדות, מנהל מוסד יראה רק את המשימות המשויכות למוסד שלו; הנהלת הרשת ומנהל-העל רואים את הפרויקט במלואו.

**הבהרה חשובה - מה כן קבוע בקוד ומה לא:** ארבעת *סוגי* ההרשאה למעלה (מי מותר לו מה) הם חלק מלוגיקת המערכת, כמו בכל מערכת הרשאות - זה לא "hardcode" במובן הבעייתי, זה כלל עסקי. לעומת זאת, **מי** בפועל משויך לכל תפקיד, **אילו** מוסדות קיימים, **אילו** boards קיימים והגדרת השדות שלהם, ו**אילו** ערכי סטטוס/עדיפות מוצגים - כל אלה הם דאטה טהור, ללא אף שורה קבועה מראש בקוד או שמוזנת ידנית לבסיס הנתונים על ידי מפתח. כל זה מוגדר ונערך אך ורק דרך ממשק הניהול המתואר בסעיף 3.

---

## 3. ממשק ניהול עצמי (Admin Panel)

זהו הרכיב שמאפשר למנהל הרשת להריץ את המערכת בעצמו מהיום הראשון, ללא תלות במפתח לכל שינוי נתונים.

### 3.1 מסך ניהול מוסדות
CRUD מלא: הוספת מוסד (שם, ואפשר שדות מטא נוספים כמו כתובת/איש קשר בעתיד), עריכה, השבתה (ולא מחיקה קשיחה, כדי לשמר היסטוריה של משימות ומשתמשים ששייכים למוסד שהושבת).

### 3.2 מסך ניהול משתמשים
מנהל-על מזמין משתמש חדש באמצעות כתובת מייל (Supabase `inviteUserByEmail`), משייך אותו לתפקיד ולמוסד, יכול לשנות תפקיד/מוסד בהמשך, ולהשבית/להפעיל מחדש משתמש. אין הרשמה עצמאית פתוחה - הצטרפות למערכת מתבצעת רק דרך הזמנה, כדי לשמור על שליטה מלאה ברשימת המשתמשים.

### 3.3 מסך הגדרות מערכת
עריכת רשימת ערכי הסטטוס (למשל: לביצוע/בתהליך/הושלם/באיחור) ורשימת ערכי העדיפות ברמת הרשת כולה - שם כל ערך וסדר התצוגה שלו. אלה מוצגים כברירת מחדל אך ניתנים לשינוי מלא דרך המסך הזה, לא נקבעים בקוד.

### 3.4 יצירת Boards והגדרת שדות מותאמים
משתמש עם הרשאה מתאימה (מנהל-על, וניתן לשקול גם הנהלת רשת) יוצר board חדש דרך הממשק, משייך אותו למוסד או מסמן ככלל-רשתי, ומגדיר את 2-3 השדות המותאמים שלו (שם, סוג, אפשרויות בחירה) - נשמר בטבלת `board_custom_field_definitions` (סעיף 10). אין צורך במפתח שמריץ סקריפט הקמה עבור board חדש.

### 3.5 ה"ביצה והתרנגולת" של מנהל-העל הראשון

יש נקודה טכנית אחת שלא ניתן לפתור דרך הממשק בלבד: ברגע ההקמה הראשוני, אין עדיין אף משתמש שיכול "להזמין" משתמשים אחרים. הפתרון המקובל (מנגנון bootstrap חד-פעמי טכני, לא hardcode של נתוני עסק): המשתמש הראשון שנרשם במערכת, או משתמש שכתובת המייל שלו תואמת למשתנה סביבה ייעודי שמוגדר בפריסה (`INITIAL_ADMIN_EMAIL`), הופך אוטומטית ל-Super Admin בהרשמתו. מרגע זה, כל שאר הניהול מתבצע דרך המסכים לעיל.

### 3.6 העברת בעלות (Ownership Transfer)

לעיתים קרובות מי שמבצע את ההקמה הטכנית הראשונית (איש IT, מפתח, או מי שנרשם ראשון בפועל) אינו מנהל הרשת עצמו. לכן, מסך ניהול המשתמשים (3.2) כולל פעולה ייעודית **"העברת בעלות"**:

- מנהל-על בוחר משתמש קיים אחר במערכת (שכבר הוזמן והצטרף) ומקנה לו הרשאת מנהל-על באופן מיידי.
- באותה פעולה, מנהל-העל היוצא יכול לבחור להסיר מעצמו את הרשאת מנהל-העל, כך שההעברה תהיה מלאה ולא רק שיתוף התפקיד.
- **הגנה מובנית:** המערכת לא תאפשר הסרת הרשאת מנהל-העל האחרון שנותר בה - תמיד חייב להישאר לפחות מנהל-על פעיל אחד, כדי למנוע מצב שבו אף אחד לא יכול להיכנס לממשק הניהול.
- שימוש טיפוסי: מי שביצע את ה-bootstrap הראשוני (סעיף 3.5) מקים את המערכת - יוצר מוסדות ראשוניים, מזמין משתמשים - ולאחר מכן מעביר את הבעלות למנהל הרשת בפועל.

---

## 4. מבנה המערכת: Boards

- כל מוסד (ו/או צוות) מקבל **board** משלו.
- קיים גם board כלל-רשתי למשימות שחוצות מוסדות.
- משתמש עם הרשאה מתאימה יוצר board חדש דרך ממשק הניהול (סעיף 3.4) - ללא התערבות מפתח.
- בשלב ה-MVP, מבנה ה-board עצמו (עמודות סטטוס, תצוגה) **קבוע** ואחיד לכל ה-boards - הגמישות היא ברמת המשימה הבודדת (סעיף 5) וברמת רשימות הסטטוס/עדיפות הגלובליות (סעיף 3.3), לא ברמת מבנה ה-board.

---

## 5. משימות (Tasks)

### שדות קבועים בכל משימה:
- כותרת
- תיאור
- סטטוס (מתוך רשימת הערכים שהוגדרה במסך הגדרות המערכת - סעיף 3.3)
- עדיפות (מתוך רשימת הערכים שהוגדרה במסך הגדרות המערכת - סעיף 3.3)
- אחראי (assignee) - משתמש יחיד או כמה (טבלת קישור `task_assignees` - סעיף 10)
- תאריך יעד
- board משוייך
- שיוך למוסד (אם רלוונטי)

### שדות מותאמים אישית (custom fields):
- כל board יכול להוסיף עד **2-3 שדות מותאמים** למשימות שלו (טקסט, מספר, תאריך, או רשימת בחירה), מוגדרים על ידי יוצר ה-board דרך הממשק (סעיף 3.4).
- כל משימה שומרת את *הערכים* שלה בעמודת JSONB, בהתאם להגדרה של ה-board שלה.

### יומן פעילות בסיסי (מומלץ ל-MVP)
מעקב אחר שינויי סטטוס ושינויי אחראי במשימה (מי שינה, מתי, מה היה לפני/אחרי) - trigger בסיסי בבסיס הנתונים, בעל ערך גבוה לאחריותיות ניהולית ומאמץ פיתוח נמוך יחסית.

### תגובות וקבצים מצורפים (מחוץ להיקף ה-MVP)
נשארים מחוץ להיקף שלב 1-2 בכוונה, כדי לשמור על מיקוד ה-POC. מומלץ לציין זאת במפורש בהצגה למנהל הרשת כהחלטת scope מכוונת.

---

## 6. פרויקטים

- קיבוץ של מספר משימות סביב יעד משותף (למשל: "הכנת שנת הלימודים תשפ״ז").
- לפרויקט: כותרת, תיאור, תאריך יעד, סטטוס התקדמות מחושב (אחוז משימות שהושלמו, במשקל שווה לכל משימה).
- פרויקט יכול לצלוח מספר boards (משימות ממוסדות שונים תחת אותו פרויקט כלל-רשתי).

---

## 7. דשבורד ניהולי

מיועד להנהלת הרשת ולמנהל-העל:
- תמונת מצב כללית: כמה משימות פתוחות/בתהליך/הושלמו/באיחור
- פילוח לפי מוסד
- פילוח לפי אחראי (עומס עבודה - נספר לפי מספר משימות פתוחות המשויכות לאותו אחראי)
- רשימת משימות באיחור, ממוינת לפי דחיפות
- התקדמות פרויקטים פעילים

---

## 8. התראות

- התראות מייל אוטומטיות על:
  - משימה שמתקרבת לתאריך היעד (למשל יום לפני)
  - משימה שעברה את תאריך היעד ועדיין פתוחה
- מנגנון: משימה מתוזמנת (Vercel Cron Job) שרצה פעם ביום, בודקת משימות רלוונטיות ושולחת מיילים.
- שליחת המייל בפועל דרך שירות טרנזקציוני חיצוני (ראו סעיף 9), ולא דרך ה-SMTP המובנה המוגבל של Supabase Auth.

---

## 9. ארכיטקטורה טכנית

| רכיב | טכנולוגיה | הערות |
|---|---|---|
| Frontend + Backend | Next.js (React + API Routes) | קוד מאוחד, Node.js "מתחת למכסה", מתארח כולו ב-Vercel |
| בסיס נתונים | Supabase (PostgreSQL מנוהל) | כולל טבלה גמישה (JSONB) עבור השדות המותאמים אישית |
| הרשאות | Supabase Row Level Security (RLS) | אכיפת הפרדת מוסדות ותפקידים ברמת בסיס הנתונים - ראו הנחיית מימוש למטה |
| התחברות | Supabase Auth (מייל+סיסמה בשלב א׳, הצטרפות בהזמנה בלבד) | ניתן להוסיף Microsoft OAuth בהמשך, ללא שינוי ארכיטקטוני |
| שליחת מיילים (הזמנות + התראות) | שירות SMTP/API חיצוני (למשל Resend) | מוגדר גם כ-Custom SMTP עבור Supabase Auth (הזמנות משתמשים) וגם נקרא ישירות מה-Cron Job (התראות תפעוליות) - פתרון אחד לשני הצרכים |
| התראות מתוזמנות | Vercel Cron Jobs | ריצה יומית, ללא צורך בשרת שרץ ברקע |
| הרצה/אחסון | Vercel (טיר חינמי - Hobby) | דיפלוי אוטומטי מ-Git, HTTPS מובנה - לבדוק תנאי שימוש עדכניים לפרויקט ארגוני |

**עלות בשלב ה-POC:** ₪0 (טיירים חינמיים בלבד).

**מעבר עתידי לתשלום/היקף גדול יותר:** אותה ארכיטקטורה בדיוק, שדרוג תוכניות Vercel/Supabase בלבד - ללא צורך ב-migration או שכתוב קוד.

### סביבות עבודה: Dev/Test מול Production (חדש)

ההפרדה בין פיתוח/בדיקות ל-production נשענת על שני מנגנונים, בלי תשתית נוספת מעבר למה שכבר קיים בארכיטקטורה:

- **Vercel:** כל push לענף `main` מתפרסם כ-Production Deployment (הכתובת הקבועה). כל branch או Pull Request אחר מקבל אוטומטית Preview Deployment משלו עם URL ייחודי, ללא הגדרה ידנית. משתני הסביבה (API keys וכו') מוגדרים בנפרד לכל סוג environment (Production / Preview / Development) בדשבורד Vercel.
- **Supabase:** יכולת ה-branching של בסיס הנתונים היא פיצ'ר בתשלום, ולכן ב-POC נעבוד עם **שני פרויקטי Supabase נפרדים** - אחד ל-production ואחד משותף ל-dev/test. כל Preview Deployment ב-Vercel מתחבר לפרויקט ה-dev/test (דרך משתני הסביבה של Preview environment), וה-production deployment מתחבר לפרויקט הנפרד. זה גם תואם את מגבלת התוכנית החינמית (בדרך כלל שני פרויקטים חינמיים לחשבון) ומספיק לחלוטין להיקף הנוכחי - שלוש סביבות נפרדות (dev/test/prod) הן over-engineering ביחס ל-POC.
- **נתוני בדיקה:** בפרויקט ה-dev/test נכנסים דרך ממשק הניהול (סעיף 3) ויוצרים מוסדות ומשתמשי-דמה, בדיוק כפי שמנהל הרשת האמיתי יעשה ב-production - בהתאם לעיקרון שאין נתונים קבועים בקוד.
- **זרימת עבודה מומלצת:** פיתוח על branch נפרד → Preview URL מחובר לפרויקט ה-dev/test → בדיקה שם → מיזוג ל-`main` → עלייה אוטומטית ל-production עם הפרויקט האמיתי.

### הערות תפעוליות
**"הירדמות" פרויקט Supabase:** פרויקט בתוכנית החינמית נכנס אוטומטית למצב מושהה אחרי כשבוע של חוסר פעילות. כדאי לוודא שהמערכת פעילה לפני כל פגישת הדגמה, או להגדיר רענון תקופתי אוטומטי שמונע את ההירדמות.

### הנחיית מימוש ל-RLS
דפוס נפוץ ובעייתי ב-Supabase הוא בדיקת תפקיד המשתמש באמצעות שאילתה חוזרת לטבלת `users` שגם לה יש RLS - עלול לגרום לרקורסיה אינסופית או לפגיעה בביצועים. ההמלצה: לשמור role ו-institution_id כ-custom claims בתוך ה-JWT (באמצעות Auth Hooks של Supabase), כך שה-policies בודקים ישירות מתוך הטוקן. חשוב שהצוות המפתח יכיר את הדפוס הזה לפני כתיבת ה-policies הראשונות.

---

## 10. מבנה נתונים

**טבלאות עיקריות:**
- `users` - משתמשים, כולל תפקיד (role) ושיוך למוסד - מאוכלסת אך ורק דרך מסך ניהול המשתמשים (סעיף 3.2) ומנגנון ה-bootstrap (סעיף 3.5)
- `institutions` - רשימת המוסדות ברשת - מאוכלסת אך ורק דרך מסך ניהול מוסדות (סעיף 3.1)
- `boards` - boards, משוייכים למוסד או מסומנים ככלל-רשתיים - נוצרים דרך הממשק (סעיף 3.4)
- `tasks` - משימות, כולל שדה `custom_fields` מסוג JSONB לשדות המותאמים אישית
- `projects` - פרויקטים
- `task_project_links` - טבלת קישור בין משימות לפרויקטים (many-to-many)
- `task_assignees` - טבלת קישור many-to-many בין `tasks` ל-`users`
- `board_custom_field_definitions` - הגדרת השדות המותאמים של כל board (שם, סוג, אפשרויות בחירה) - נערכת דרך סעיף 3.4
- `task_activity_log` - רשומת שינויי סטטוס/אחראי (משימה, שדה, ערך קודם/חדש, מי, מתי)
- `system_settings` - רשימת ערכי הסטטוס והעדיפות הגלובליים ברמת הרשת, נערכת דרך סעיף 3.3

**עיקרון מנחה:** אף אחת מהטבלאות האלה אינה מקבלת שורות התחלתיות (seed) קבועות בקוד או במיגרציה - כל הנתונים מוזנים דרך ממשק הניהול לאחר הפריסה, פרט למנגנון ה-bootstrap החד-פעמי של מנהל-העל הראשון (סעיף 3.5), שגם ממנו ניתן להעביר בעלות בהמשך (סעיף 3.6). כל זה חל בנפרד על כל אחד משני פרויקטי Supabase (production ו-dev/test).

---

## 11. שלבי ביצוע (Roadmap)

| שלב | תכולה |
|---|---|
| **שלב 1 - ליבה (MVP להדגמה)** | הקמת ריפו GitHub, שני פרויקטי Supabase (production + dev/test) וחיבור Vercel Preview/Production, טבלאות בסיס, RLS מבוסס JWT claims, **ממשק ניהול עצמי מלא** (מוסדות, משתמשים+הזמנות, הגדרות סטטוס/עדיפות, יצירת boards+שדות מותאמים, העברת בעלות), מנגנון bootstrap למנהל-על ראשון, יצירת/הקצאת/עדכון משימות, boards מרובים, פרויקטים בסיסיים, יומן פעילות בסיסי |
| **שלב 2** | דשבורד ניהולי, התראות מייל (כולל שילוב שירות המייל החיצוני כ-Custom SMTP + API) |
| **שלב 3** | ליטוש UI/UX, בדיקות עם משתמשים אמיתיים, תגובות/קבצים מצורפים אם יידרש |
| **שלב 4 (לאחר אישור מנהל הרשת)** | מעבר לתוכניות בתשלום, SSO מול Microsoft Entra ID, אינטגרציית Teams/Outlook |

---

## נקודות פתוחות לדיון

**נותרו פתוחות:**
- האם קיימים כרגע נתוני משימות במקום אחר (אקסל, וואטסאפ, נייר) שצריך לייבא בהקמה, או שמדובר בהתחלה נקייה לגמרי?
- האם ההנחה (סעיף 1) על web בלבד, ללא אפליקציית מובייל נייטיבית, מקובלת לשלב ה-POC?
- האם נדרש מנגנון "סל מיחזור" למשימות/boards שנמחקו בטעות, או שמחיקה היא סופית?

**נסגרו:**
- ~~רשימת המוסדות הראשונית~~ - מוזנת על ידי מנהל-העל דרך מסך ניהול מוסדות.
- ~~מי יהיה מנהל-העל הראשון~~ - `elhanan.klein.lognet@gmail.com` הוגדר כ-`INITIAL_ADMIN_EMAIL` (סעיף 3.5), עם אפשרות להעביר את הבעלות בהמשך (סעיף 3.6).
- ~~הגדרה מדויקת של שמות הסטטוסים והעדיפויות~~ - ניתנים לעריכה מלאה דרך מסך הגדרות המערכת (סעיף 3.3).
- ~~הפרדת סביבות dev/test/prod~~ - Vercel Preview/Production + שני פרויקטי Supabase (סעיף 9).
